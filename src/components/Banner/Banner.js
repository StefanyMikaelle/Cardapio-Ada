import './Banner.css'

export const Banner = () => {
    // JSX
    return (
        <header className="banner">
            <img src="/images/image-header-title.png" alt="O banner principal da página do Cardápio do Início"/>
        </header>
    )
}